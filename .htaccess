RewriteEngine On
RewriteBase /mrs.millet.customer/

# -----------------------------------------
# Remove .php extension from URL
# Example:
# /mrs.millet.customer/login.php
# becomes:
# /mrs.millet.customer/login
# -----------------------------------------
RewriteCond %{THE_REQUEST} \s/+mrs\.millet\.customer/(.+?)\.php[\s?] [NC]
RewriteCond %{REQUEST_METHOD} GET
RewriteRule ^(.+)\.php$ /mrs.millet.customer/$1 [R=301,L]

# -----------------------------------------
# Don't rewrite existing files or folders
# -----------------------------------------
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# -----------------------------------------
# Automatically load .php files
# Example:
# /mrs.millet.customer/login
# loads:
# login.php
# -----------------------------------------
RewriteRule ^(.+)$ $1.php [L,QSA]

# -----------------------------------------
# Default homepage
# -----------------------------------------
RewriteRule ^$ index.php [L]

# -----------------------------------------
# Don't rewrite AJAX POST requests
# -----------------------------------------
RewriteCond %{REQUEST_URI} ^/mrs\.millet\.customer/ajax/
RewriteCond %{REQUEST_METHOD} POST
RewriteRule ^ - [L]